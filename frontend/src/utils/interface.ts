export interface MobileMenuProps {
    isOpen: boolean;
}

export interface ToggleButtonProps {
    active: string;
    value: string;
}
export interface ContactInputMessageProps {
    rows: string;
}

export interface ButtonProps {
    dull: string;
}
